# 更新日志

## 0.3.0（2026-09-24）

- 内置节点代理：设置里新增「节点与订阅」页，填机场的订阅地址就能在面板里选节点、自动选择、测速；配置列表里自动多一条「节点代理」，开关它就是开关内置代理。
- 全局代理和规则分流两种模式；规则来源可以选内置「国内直连」、johnshall 的小火箭规则预设（黑名单 / 白名单 / 去广告 / 国内 IP 直连），或任何小火箭、Surge、Clash 格式的规则地址，`RULE-SET` 引用会下载后内联。
- 右键菜单里新增「节点」子菜单，可以切模式、选节点。
- 内核 mihomo v1.19.31（通用二进制）和 GeoIP 数据库打包在 app 里，程序退出时内核一起退出。

## 0.2.0（2026-09-24）

- 检查更新：启动后和每 6 小时检查一次 GitHub 上的新版本（通用页可以关掉），有新版本时通知一次；「关于」页、右键菜单和主菜单里可以手动检查，能看到版本说明，也可以跳过某个版本。
- 一键更新：面板里的更新条或「关于」页点「更新」，自动下载 zip、比对 `SHA256SUMS.txt` 里的 SHA-256、校验签名、就地替换 `ProxySwitch.app` 并重新启动。目录没有写权限时走系统的授权对话框。
- 新命令 `open proxyswitch://update`（检查并安装），`proxyswitch://settings?page=about` 可以直接打开某一页。
- iCloud 同步：设置里新增「iCloud 同步」页，打开后代理配置和设置通过 iCloud 云盘在多台 Mac 之间同步；另一台 Mac 开启时可以选用 iCloud 的、用本机的或合并；两边同时改动时以时间晚的为准，iCloud 留下的冲突版本自动处理。

## 0.1.1（2026-09-24）

- 修复设置窗口里 ⌘C / ⌘V / ⌘A / ⌘Z 等快捷键不起作用：补上主菜单，设置窗口打开期间程序临时切成普通应用，菜单栏里能看到「编辑」菜单。
- 地址输入：把 `127.0.0.1:7890`、`socks5://127.0.0.1:1080` 这样的整段地址粘到「主机」里会自动拆成类型、主机、端口；端口只接受数字；在表单里按回车直接保存。

## 0.1.0（2026-09-24）

第一个版本，从 [proxyswitch](https://github.com/whrss9527/proxyswitch)（Windows 版）仓库拆出。

- 菜单栏面板：大开关、配置列表和延迟、复制终端命令、测速、进设置；右键简洁菜单。
- 配置：HTTP / SOCKS5 / PAC，生效范围可选系统代理、环境变量（launchd）、git、npm。
- 系统代理：SystemConfiguration 读取和监听，别的程序改了代理会立刻反映，可以一键保存成配置；networksetup 写入，标准账户走系统授权对话框。
- 全局快捷键（默认 ⌃⌥P）、登录时启动、代理服务器连不上时提醒、自动检测本机代理软件、`proxyswitch://` 命令。
- 设置窗口：透明标题栏、侧栏导航、配置编辑器、快捷键录制、诊断、关于；用 Xcode 26 编译时在 macOS 26 上启用 Liquid Glass。
